{
  "name": "zeb-echo-proxy",
  "version": "0.1.0",
  "description": "Cloudflare Worker that proxies Workers AI calls for zeb Echo, holding the API token server-side so the desktop app ships no secret.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250620.0",
    "typescript": "^5.7.2",
    "wrangler": "^4.20.0"
  }
}
